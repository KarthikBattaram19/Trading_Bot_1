# Bot Health Report — Preview / First Run Sample

| Field | Value |
|---|---|
| Generated | 2026-08-02 (manual dry run, ahead of skill build) |
| Reviewed range | Full history — `046440f` (Initial Phase 0) → `31e928f` (HEAD), 51 commits, 2026-07-30 → 2026-08-02 |
| Purpose | Preview of the report format the **`Guruji_for_Bhale_Bullodu`** skill will produce, using real evidence from this repo, so you can approve the shape before it's built as a skill |

This is **not** generated by the skill yet — it's a manual run of the same checklist the skill design specifies, to show you what it looks like in practice.

---

## 1. Change digest (since project start)

| Area | What landed |
|---|---|
| Options-only lock | Hard-locked recommend/paper/signals/broker paths to Call/Put legs only; `OPTIONS_ONLY_REQUIRED` reject code added across structure_builder, paper_sim, signals, recommendations |
| Liquidity / ATM gates | Relative ATM liquidity gates for volume/OI/spread; premium and confidence-floor tuning |
| Paper sim | Phase 1 stack shipped (ledger, engine, fill model, market feed, automation, freshness); options-only rehedge enforced |
| Recommendations / learning | Outcome-calibrated confidence maps; `/learning` seeded with demo outcomes; live-clean quant snapshots + per-strategy coverage gates |
| Market data | Live ICICI Direct LTP + option-chain marks for all FNO underlyings; NIFTY 50 / India VIX wired live; India Market_News RSS ingestion |
| RAG / chat | Track B1 RAG chat shipped end-to-end, then un-shipped ("un-implement") pending a rebuild plan |
| Frontend | Cockpit UI refresh; IST timestamps; feed status typing fix |
| Deploy/CI | Railway/Vercel deploy fixes (Railpack, monorepo root, analytics colocation); CI gate fixes |

**Read:** very high commit velocity (51 commits / 3 days) concentrated on quant correctness and the options-only safety lock, not yet on the supervised-approval loop or live-safety durability (see §3).

## 2. Engineering health

| Check | Result |
|---|---|
| Test suite | `pytest -q` → **223 passed**, 0 failed, 1 unrelated deprecation warning (`httpx`/starlette) |
| Options-only invariant | `OPTIONS_ONLY_REQUIRED` enforced in `structure_builder`, `paper_sim` (ledger/engine/automation), signals evaluate path — grep-verified, matches architecture.md claim |
| One-trade-at-a-time (§20.4.11) | Not independently verified this pass — flag for next run |
| `SUPERVISION_MODE` default | Documented as `supervised` in architecture.md §1.2/§2.3.1 — code-side default not cross-checked this pass |
| Doc/code drift | `architecture.md` v1.28 and `context.md` v3.9 both dated 2026-08-01, ahead of `17b7e6c`/`31e928f` — no material drift found, docs appear to track code closely for the options-only lock work |

## 3. P0 → P2 checklist (`.cursor/rules/must-fix-before-claiming-performance.mdc`)

Each item's Definition of Done is quoted from the rule file itself — Guruji checks against that wording, not a paraphrase.

| # | Item | Status | Evidence |
|---|---|---|---|
| P0-1 | Wire recommend → supervised approve → `paper_sim` → learning; one ledger as source of truth; real approve/reject APIs | **Not done** | `backend/routers/decisions.py:1` — module docstring literally states *"Read-only decision log (Phase 0) — no approve / reject transitions yet."* Only `GET` endpoints exist (list/pending/get); no `POST /approve` or `/reject`. `backend/data/learning_store.json` currently holds only seeded demo data (`"seed": true` on all entries), not real closed trades from the loop. DoD requires zero seed records feeding reported metrics — currently 100% of records are seed. |
| P0-2 | Enforce breakers + market hours + durable kill/positions | **Partial** | Pre-trade risk gate (`backend/execution/risk_gate.py`, incl. `market_hours` check) and daily-loss circuit breaker (`backend/execution/circuit_breakers.py`) *are* wired into `backend/paper_sim/engine.py:10,625` — satisfies the "checks run inside the submit path" half of the DoD. But kill-switch armed state and the open-position book are plain in-memory module globals (`backend/routers/bot.py:24` `_kill_switch_armed = False`) with no persistence — fails the "must resume in the same state after restart" half. **Superseded 2026-08-05:** the kill-switch mechanism was removed entirely per operator decision (no manual kill switch, no replacement); only the open-position-book persistence half of this row's finding remains open. See `Docs/superpowers/specs/2026-08-05-market-news-quality-killswitch-removal-design.md`. |
| P1-3 | Options-chain walk-forward / replay for SH-4 expectancy (OOS evidence) | **Not done** | No walk-forward/replay script or train/test-split PnL artifact found in `backend/` or `Docs/`. Learning store has 0 real closed trades (all seeded), so no OOS slice could exist yet regardless. |
| P1-4 | Skew / term / India VIX regime filters + IV quality | **Not done** | India VIX level is live-wired (§1), but `grep -rli "skew\|term_structure" backend/quant` returns no matches — no dedicated skew/term-structure module exists yet, so the DoD's "gates or adjusts strategy selection using them" isn't met. |
| P2-5 | Structure sizing (delta/vega targets, lots, costs → expectancy after costs) | **Partial** | `backend/quant/costs/transaction_cost.py` and `backend/quant/risk/greeks_limits.py` exist, so cost and Greeks-limit infrastructure is present — but this pass didn't confirm they feed an explicit delta/vega-target sizing calc surfaced in ranking/expectancy, which the DoD requires. Needs a follow-up read of `backend/quant/gamma/hedge_optimizer.py` and the recommendation ranking path. |
| P2-6 | Fill / reconcile FSM before live micro-capital | **Not done** | `grep -rli "reconcile\|fill_state\|order_state" backend --include=*.py` (excluding tests) returns no matches — no explicit submitted→partial→filled/rejected→reconciled state machine found. |

**Claim-gate check:** P0 is not fully Done and P1-3 has no OOS evidence → **no "edge proven" / "top-retail performance" language is warranted anywhere right now**, including in the docs' own framing. Nothing currently in the repo violates this, but it's the bar any future "performance" claim needs to clear.

## 4. Trade metrics

Not weighted into this report — `learning_store.json` contains 0 real closed trades (only seed/demo data), far below the architecture's own 30-closed-trades-per-module promotion threshold. Per the design, metrics stay engineering-health-led until real paper_sim closes start accumulating.

## 5. Backlog (would be written to `Docs/bot_health/BACKLOG.md`)

**P0 — open**
- [ ] Build real `POST /approve` and `POST /reject` endpoints in `decisions.py`, make `paper_sim` ledger the single source of truth, and exclude seed/demo records from `/learning` metrics (first seen 2026-08-02)
- [ ] Persist kill-switch armed state and the open-position book so they survive a process restart — currently in-memory globals only in `bot.py` (first seen 2026-08-02)
  **Superseded 2026-08-05:** the kill-switch mechanism was removed entirely per operator decision; only the open-position-book persistence half of this item remains open. See `Docs/superpowers/specs/2026-08-05-market-news-quality-killswitch-removal-design.md`.

**P1 — open**
- [ ] No walk-forward/OOS replay evidence exists yet for SH-4 expectancy claims — blocked on P0-1 producing real closed trades to replay against (first seen 2026-08-02)
- [ ] No skew/term-structure regime filter module exists under `backend/quant` — India VIX level alone doesn't meet the DoD (first seen 2026-08-02)

**P2 — open**
- [ ] Confirm whether existing cost/Greeks-limit modules (`transaction_cost.py`, `greeks_limits.py`, `hedge_optimizer.py`) actually feed an explicit delta/vega-target sizing calc in ranking, or whether that wiring still needs building (first seen 2026-08-02, needs follow-up read)
- [ ] No fill/reconcile state machine found — required before any live micro-capital phase (first seen 2026-08-02)

**Other — deferred behind open P0/P1**
- [ ] RAG chat was shipped then un-shipped pending a Track B rebuild plan — not blocking, tracked for awareness only

## 6. Next best action

Per P0 > P1 > P2 ordering: the single highest-leverage next step is **P0-1** — wiring real approve/reject transitions in `decisions.py` onto the `paper_sim` ledger as one source of truth. Until that's real, `/learning` outcomes stay synthetic and nothing built on top of them (confidence calibration, win-rate reporting) reflects the actual loop.

---

*This file is a one-time preview to validate the report format. Once approved, `Guruji_for_Bhale_Bullodu` will regenerate reports like this on its own trigger and maintain `STATE.md` / `BACKLOG.md` going forward instead of this static file.*
